<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.one.afternoon.mapper.ConfigMapper">

    <!--    字段:-->
    <!--    @TableId(type = IdType.AUTO)-->
    <!--    private Long id;-->
    <!--    private String configname;-->
    <!--    private String password;-->
    <!--    private Integer age;-->
    <!--    private String sex;-->
    <!--    private String role;-->
    <!--    private Integer online;-->
    <!--    @TableField(fill = FieldFill.INSERT) //设置自动生成-->
    <!--    private Date createAt;-->
    <!--    @TableField(fill = FieldFill.INSERT_UPDATE) //设置自动修改/生成-->
    <!--    private Date updateAt;-->

    <!--查询student表中所有的数据-->
    <select id="findAll" resultType="org.one.afternoon.domain.Config">
        SELECT * FROM config
    </select>


    <!--插入操作-->
    <insert id="insert" parameterType="org.one.afternoon.domain.Config">
        INSERT INTO config (user_id,config)
        VALUES (#{userId},#{config})
    </insert>

    <!--根据id删除-->
    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM config WHERE user_id=#{listingId}
    </delete>

    <!--更新操作-->
    <update id="update" parameterType="org.one.afternoon.domain.Config">
        UPDATE config SET
        config=#{config}
        where user_id=#{userId}
    </update>
</mapper>